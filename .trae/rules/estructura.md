# Directory Structure
- The project is a monorepo.
- Backend logic and dependencies are in `/backend`.
- Frontend logic and dependencies are in `/frontend`.
- ALWAYS run commands from their respective subdirectories.
- When adding Python packages, use `pip install` inside `/backend` and update `requirements.txt`.
- When adding JS packages, use `npm install` inside `/frontend`.